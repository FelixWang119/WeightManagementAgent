---
name: redesign-calorie-balance-scale
overview: 重新设计热量平衡卡片为天平对比式布局，直观展示摄入端vs消耗端的对比关系
design:
  architecture:
    framework: html
  styleKeywords:
    - 天平对比
    - 卡片布局
    - 渐变色彩
    - 圆角设计
    - 状态可视化
  fontSystem:
    fontFamily: PingFang-SC
    heading:
      size: 1rem
      weight: 600
    subheading:
      size: 0.9rem
      weight: 500
    body:
      size: 0.875rem
      weight: 400
  colorSystem:
    primary:
      - "#4CAF50"
      - "#FF6B6B"
      - "#2196F3"
    background:
      - "#FFFFFF"
      - "#F8F9FA"
      - "#E8F5E9"
      - "#FFF3E0"
    text:
      - "#333333"
      - "#666666"
      - "#FFFFFF"
    functional:
      - "#4CAF50"
      - "#FF6B6B"
      - "#FF9800"
todos:
  - id: replace-calorie-card
    content: 替换热量平衡卡片为对比式布局
    status: pending
---

## 产品概述

重新设计首页热量平衡卡片，采用"天平对比式"布局，直观展示"饮食摄入 < 基础代谢 + 运动消耗"的减肥核心公式。

## 核心功能

- 左右对比布局：左侧展示"摄入端"（饮食摄入），右侧展示"消耗端"（基础代谢+运动消耗）
- 中间核心区域：显示热量缺口/盈余数值和减脂/增重状态
- 底部计算公式：展示"基础代谢 + 运动消耗 = 总消耗"和剩余可摄入量
- 颜色区分状态：绿色表示缺口（减脂），红色表示盈余（增重），蓝色表示平衡
- 保留无BMR引导提示和加载状态

## 技术栈

- 前端：HTML + CSS + JavaScript（原生）
- 样式：CSS Flexbox/Grid布局，渐变背景，动画过渡
- 数据：复用现有API接口 `/api/calories/balance/daily`

## 实现方案

### 架构设计

- 替换现有 `.balance-detail` 内部结构，保持外层卡片容器不变
- 新增天平对比布局CSS类：`.balance-scale-container`、`.scale-side`、`.scale-center`
- 修改 `displayCalorieBalance()` 函数，适配新的DOM结构
- 保留现有的 `loadCalorieBalance()`、`setStatusIndicator()`、`showHistoryComparison()` 函数逻辑

### 关键变更

1. **HTML结构**：将进度条+卡片布局替换为左-中-右三栏天平布局
2. **CSS样式**：

- 左右两侧使用渐变卡片展示摄入和消耗
- 中间使用圆形徽章展示缺口/盈余数值
- 底部添加公式说明区域

3. **JavaScript**：

- 更新DOM元素ID映射
- 添加BMR数据展示逻辑
- 优化状态判断和颜色切换

### 目录结构

```
static/index.html  # [MODIFY] 替换热量平衡卡片HTML结构和样式
```

## 实现细节

- 保持与现有代码风格一致，使用CSS变量
- 响应式设计，适配移动端
- 动画过渡效果，提升用户体验
- 保留历史对比功能（可选显示）

## 设计风格

采用"天平对比式"设计，强调摄入与消耗的直观对比。使用卡片式布局，圆角设计，渐变色彩，现代简约风格。

## 布局结构

```
┌─────────────────────────────────────┐
│  🔥 今日热量平衡          2月10日    │  <- 头部（保持现有）
├─────────────────────────────────────┤
│                                     │
│   ┌────────┐    ⚖️    ┌────────┐   │
│   │  🍽️    │   缺口   │   🔥   │   │
│   │  摄入  │  ┌───┐   │  消耗  │   │
│   │ 1200  │  │600│   │ 1800  │   │
│   │  kcal │  │缺口│   │  kcal │   │
│   │        │  └───┘   │        │   │
│   └────────┘          └────────┘   │
│      绿色/红色提示状态              │
│                                     │
│  基础代谢 1400 + 运动 400 = 1800    │
│  还可以吃 600 kcal                  │
│                                     │
└─────────────────────────────────────┘
```

## 样式关键词

- 天平对比布局
- 卡片式数据展示
- 渐变色彩区分状态
- 圆形中心徽章
- 圆角现代设计